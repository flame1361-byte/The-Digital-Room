# TheDigitalRoom - Environment Variables Template
# Copy this to the "Environment Variables" section of your hosting provider (Render, Koyeb, etc.)

# Secret for signing login tokens (Make this something long and random)
JWT_SECRET=super_secret_key_change_me

# The username of the room owner (Full admin permissions)
ADMIN_USER=mayne

# (Optional) Configurable path for the NeDB database file
# If using Render Free, this will reset on every deploy.
# DATABASE_PATH=/opt/render/project/src/data/users.db
