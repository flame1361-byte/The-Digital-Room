# TheDigitalRoom - Environment Variables Template
# CRITICAL: This file MUST be configured before production deployment
# Copy and update all values in your hosting provider's environment settings

# ============================================================================
# REQUIRED VARIABLES (Application will not start without these)
# ============================================================================

# JWT Secret: Use a long, random string for signing authentication tokens
# SECURITY: Must be at least 32 characters long in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-key-minimum-32-chars-change-this

# Admin User: Username with full administrative permissions (room owner)
# This user can kick users, clear chat, reset DJ, and make announcements
ADMIN_USER=mayne

# ============================================================================
# RECOMMENDED VARIABLES
# ============================================================================

# Port: HTTP server port (default: 3000)
PORT=3000

# Node Environment: Set to 'production' for production deployments
NODE_ENV=development

# Database Path: Path to NeDB database file
# For Render Free Tier: Data resets on each deploy, use free tier DB service instead
# DATABASE_PATH=/opt/render/project/src/data/users.db

# Allowed Origins: Comma-separated list of allowed client origins
# SECURITY: Set this to your domain in production to prevent CORS attacks
# Example: https://mysite.com,https://www.mysite.com
ALLOWED_ORIGINS=http://localhost:3000

# ============================================================================
# SECURITY REMINDERS
# ============================================================================
# - NEVER commit actual values of JWT_SECRET or ADMIN_USER to version control
# - NEVER use default values in production
# - Always use HTTPS/TLS in production
# - Set strong, unique JWT_SECRET for each environment
# - Configure ALLOWED_ORIGINS whitelist for your domain
# - Run 'npm audit' regularly and update dependencies
# ============================================================================

